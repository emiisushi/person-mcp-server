{
  "name": "person-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing Person CRUD tools backed by the Person API.",
  "type": "module",
  "bin": {
    "person-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsc -w -p ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
